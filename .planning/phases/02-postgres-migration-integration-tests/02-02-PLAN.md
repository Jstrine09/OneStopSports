---
phase: 02-postgres-migration-integration-tests
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/test/java/com/onestopsports/migration/PostgresMigrationIT.java
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [HARD-02]

must_haves:
  truths:
    - "After V9 runs over a seeded duplicate-club scenario, the two team rows sharing (sport_id, external_id) collapse into ONE canonical row (the MIN(id)) and the duplicate is deleted (roadmap criterion 3; D-04)."
    - "The canonical team is linked to BOTH competitions in team_league (the duplicate's league link is re-pointed, not lost), and its players are collapsed to one row per (team_id, name) (D-04)."
    - "favorite_team and favorite_player are re-pointed onto the canonical rows on the no-collision branch, AND on the collision branch the duplicate favourite is DELETED (not duplicated) so the (user_id, team_id)/(user_id, player_id) unique constraints hold — proven behaviorally by an attempted duplicate INSERT raising a unique violation (D-04)."
    - "`mvn verify -Pintegration` and its Docker prerequisite are documented in CLAUDE.md (and README.md) so the opt-in migration IT can be run deliberately and wired into CI (roadmap criterion 4; D-02)."
  artifacts:
    - "src/test/java/com/onestopsports/migration/PostgresMigrationIT.java — seedFixtures extended with the duplicate-club scenario, plus the exhaustive V9 merge @Test methods."
    - "CLAUDE.md — Testing section documents `mvn verify -Pintegration` + Docker prerequisite for the migration IT."
    - "README.md — a short note pointing at the integration-test command."
  key_links:
    - "The duplicate-club fixture must be inserted at the Stage-1 (target 8) point of the EXISTING @BeforeAll seedFixtures — i.e. while team.league_id still exists and team.sport_id does not yet — because V9 itself backfills sport_id from league_id before the merge; both duplicate rows must reference leagues that share the same sport_id so they resolve into one merge group."
    - "Favourite collision-skip depends on inserting, for one user, a favourite on BOTH the canonical AND the duplicate team/player pre-merge; V9's NOT EXISTS re-point keeps the canonical favourite and the trailing DELETE removes the duplicate-pointing row."
---

<objective>
Complete the exhaustive V9 data-merge verification (D-04) by extending the `PostgresMigrationIT` created in plan 02-01. This plan adds the duplicate-club fixtures to the existing `seedFixtures` method (D-03) and the `@Test` methods that assert every documented V9 merge guarantee: duplicate clubs sharing `(sport_id, external_id)` collapse into one canonical row, `team_league` is populated for the canonical club across both competitions, players are re-pointed and de-duplicated, and favourites (team AND player) are re-pointed on the no-collision branch and de-duplicated on the collision branch respecting the unique constraints. Finally it documents the opt-in `mvn verify -Pintegration` command and its Docker prerequisite in CLAUDE.md and README.md (D-02).

Together with plan 02-01 this delivers the remaining half of roadmap Success Criterion 3 (the V9 data merge) and completes Criterion 4 (documented profile). It depends on 02-01 because it edits the same IT class and the same `seedFixtures` method.

Purpose: The favourites re-pointing + unique-constraint collision-skip is the subtlest, highest-bug-risk part of V9's merge (CONTEXT.md D-04). It is only ever exercised against real Postgres, so this IT is the sole place it is proven correct.
Output: `PostgresMigrationIT.java` extended with the duplicate-club fixture + seven merge `@Test` methods, plus doc updates in CLAUDE.md and README.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-postgres-migration-integration-tests/02-CONTEXT.md
@.planning/phases/02-postgres-migration-integration-tests/02-RESEARCH.md
@.planning/phases/02-postgres-migration-integration-tests/02-PATTERNS.md
@src/main/resources/db/migration/V9__team_league_many_to_many.sql
@.planning/phases/02-postgres-migration-integration-tests/02-01-PLAN.md
</context>

<task_note>
This plan EXTENDS the existing PostgresMigrationIT.java and its seedFixtures method from plan 02-01. Read 02-01-SUMMARY.md first to see exactly how seedFixtures and @BeforeAll were structured, then add to them — do not rewrite the class or restructure @BeforeAll.
</task_note>

<tasks>

<task type="auto">
  <name>Task 1: Extend seedFixtures with the duplicate-club scenario exercising every V9 merge branch (D-03, D-04)</name>
  <files>src/test/java/com/onestopsports/migration/PostgresMigrationIT.java</files>
  <read_first>
    - .planning/phases/02-postgres-migration-integration-tests/02-01-SUMMARY.md (how 02-01 built seedFixtures + @BeforeAll — extend, don't rewrite).
    - src/main/resources/db/migration/V9__team_league_many_to_many.sql lines 54-141 (the merge SQL: canonical = MIN(id) per (sport_id, external_id) HAVING COUNT>1; player de-dupe by (team_id, name); favorite_team/favorite_player re-point via NOT EXISTS then DELETE the remainder).
    - .planning/phases/02-postgres-migration-integration-tests/02-RESEARCH.md lines 237-287 (Pattern 2: the concrete fixture recipe — 1 sport, 2 leagues sharing sport_id, 2 team rows sharing external_id '86' linked to different leagues, duplicate players, favourites covering the re-point branch AND the collision/de-dupe branch).
    - .planning/phases/02-postgres-migration-integration-tests/02-RESEARCH.md lines 247-253 (the two favourite branches: User A favourites only the duplicate → must re-point; User B favourites BOTH canonical and duplicate → duplicate favourite must be deleted).
    - src/main/resources/db/migration/V1__create_sport_league.sql, V2__create_team_player.sql, V3__create_user_favorites.sql (exact columns: team(league_id,name,external_id), player(team_id,name), user_account(username,email,password_hash), favorite_team(user_id,team_id), favorite_player(user_id,player_id)).
  </read_first>
  <action>
    Extend the EXISTING private static seedFixtures(JdbcTemplate) from plan 02-01 by appending the duplicate-club scenario — do not touch the V8/schema rows already there and do not restructure @BeforeAll (it already runs Stage 1 target 8 → seedFixtures → Stage 2 target 9, which is exactly the injection point this fixture needs). All INSERTs use parameterized JdbcTemplate `?` placeholders and queryForObject to recover generated ids.
    Build the scenario per RESEARCH.md Pattern 2: one football sport; TWO leagues under that same sport (a "Domestic League" and a "Continental Cup") so both duplicate rows resolve to the same sport_id when V9 backfills it; TWO team rows that are the duplicate club — both with the SAME external_id (use a value distinct from the plan-02-01 V8 fixture team, e.g. '86'), the lower-id row inserted first and linked via league_id to the domestic league, the higher-id row linked to the continental cup (this is canonical=MIN(id) vs duplicate). Insert the SAME player name under BOTH team rows (simulates the squad seeded once per competition — the target of V9's (team_id, name) de-dupe). Create user_account rows and favourites covering BOTH branches:
      • favorite_team re-point branch: a user who favourites ONLY the duplicate (non-canonical) team.
      • favorite_team collision branch: a user who favourites BOTH the canonical team AND the duplicate team.
      • favorite_player re-point branch and collision branch: mirror the same two shapes keyed on the duplicated player rows.
    Capture the canonical team id (MIN of the two by id), the duplicate team id, both duplicated player ids, and the user ids as fields/returned values the Task 2 assertions can read (e.g. store them in static fields set during seeding, or re-query them by external_id/name — keep it parameterized). Add junior-developer comments explaining which fixture row exercises which V9 branch (re-point vs collision-skip), per the CLAUDE.md hard rule.
  </action>
  <verify>
    <automated>mvn -q verify -Pintegration -Dit.test=PostgresMigrationIT 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - seedFixtures now inserts, in addition to the 02-01 V8 rows: 1 sport, 2 same-sport leagues, 2 team rows sharing external_id (distinct from the V8 fixture team), duplicate players under both teams, and user_account + favorite_team + favorite_player rows covering both the re-point and collision branches for teams and players.
    - The lower-id (canonical) duplicate team is linked to one league and the higher-id (duplicate) team to the other; both leagues share sport_id.
    - Canonical/duplicate team ids, duplicate player ids, and user ids are recoverable by the Task 2 assertions.
    - @BeforeAll is NOT restructured; the V8/schema fixture rows and all six 02-01 @Test methods remain and still pass.
    - All INSERTs use parameterized `?` placeholders; junior-developer comments identify which row drives which V9 branch.
    - `mvn -q verify -Pintegration -Dit.test=PostgresMigrationIT` exits 0 (fixtures compile and the existing assertions still pass over the enlarged seed).
  </acceptance_criteria>
  <done>seedFixtures is extended with the full duplicate-club scenario, the 02-01 tests still pass, and the IT is green under `mvn verify -Pintegration`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the exhaustive V9 merge assertions incl. favourites collision-skip (D-04)</name>
  <files>src/test/java/com/onestopsports/migration/PostgresMigrationIT.java</files>
  <behavior>
    - v9_duplicateClubs_mergedIntoCanonicalRow: exactly one team row remains for the shared external_id, and it is the canonical MIN(id); the duplicate id no longer exists in team.
    - v9_teamLeagueJoinTable_populatedForCanonical: team_league has links from the canonical team to BOTH leagues (domestic + continental); no team_league row references the deleted duplicate team id.
    - v9_duplicatePlayers_repointedAndDeduplicated: after merge, the canonical team has exactly ONE player row for the shared name (the per-(team_id,name) MIN(id)); the duplicate player id is gone.
    - v9_favoriteTeam_repointedToCanonical_whenNoCollision: User A's favourite now has team_id = canonical team id; no favorite_team row points at the deleted duplicate team id.
    - v9_favoriteTeam_dupDeleted_whenCanonicalAlreadyFavorited: User B ends with exactly ONE favorite_team row for the canonical team; the duplicate-pointing favourite was deleted; a fresh INSERT duplicating (User B, canonical team) raises a unique-constraint violation (SQLState 23505).
    - v9_favoritePlayer_repointedToCanonical_whenNoCollision: mirrors the team re-point assertion at the player level.
    - v9_favoritePlayer_dupDeleted_whenCanonicalAlreadyFavorited: mirrors the team collision-skip assertion at the player level, including the behavioral 23505 unique-violation check.
  </behavior>
  <read_first>
    - src/main/resources/db/migration/V9__team_league_many_to_many.sql lines 89-141 (favorite_team + favorite_player re-point-then-delete logic — the exact behavior these tests pin).
    - .planning/phases/02-postgres-migration-integration-tests/02-RESEARCH.md lines 366-369 (Pitfall 6: do NOT hardcode Postgres's auto-generated unique-constraint name; prefer a behavioral attempt-duplicate-INSERT that expects a 23505 unique violation — fk_team_sport is the only constraint safe to assert by literal name).
    - .planning/phases/02-postgres-migration-integration-tests/02-RESEARCH.md lines 360-364 (Pitfall 5: assert permanent tables only — team/player/team_league/favorite_team/favorite_player — never the V9 temp tables).
    - src/test/java/com/onestopsports/service/NbaApiServiceTest.java lines 1-40 (GIVEN/WHEN/THEN teaching-comment shape to mirror).
  </read_first>
  <action>
    Add the seven @Test methods listed in <behavior> to PostgresMigrationIT, each a read-only (except the deliberate collision INSERT) JdbcTemplate query against the migrated+merged database, asserting one V9 guarantee with AssertJ and carrying GIVEN/WHEN/THEN junior-developer comments. Use the ids captured in Task 1's seeding to target the exact canonical/duplicate rows.
    For the merge/count assertions, query the permanent tables only (Pitfall 5): COUNT team rows for the shared external_id = 1 and equals the canonical id; COUNT team_league rows for the canonical team = 2 and 0 for the duplicate id; COUNT player rows for (canonical team_id, shared name) = 1; favorite_team/favorite_player team_id/player_id now equal the canonical ids with 0 rows pointing at the deleted duplicates.
    For the two collision-skip tests, additionally prove the unique constraint holds BEHAVIORALLY (Pitfall 6): attempt a JdbcTemplate.update INSERT that would duplicate the surviving (user_id, team_id) / (user_id, player_id) pair and assert it throws (AssertJ assertThatThrownBy), and that the thrown exception's SQLState is 23505 (unique violation) — do NOT hardcode the auto-generated constraint name. Because this INSERT intentionally mutates state, run it last within its test and do not rely on row counts after it. Do not assert on the fk_team_sport name here (already covered in 02-01) except where naturally convenient.
    Keep the class name *IT and the plain-JUnit5 (no @SpringBootTest) shape from 02-01 unchanged.
  </action>
  <verify>
    <automated>mvn -q verify -Pintegration -Dit.test=PostgresMigrationIT 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All seven @Test methods from <behavior> exist and pass under `mvn verify -Pintegration`.
    - Duplicate-club merge, team_league population across both leagues, player re-point + de-dupe, and favourite re-point (no-collision) are asserted against permanent tables using the Task-1 captured ids.
    - Both collision-skip tests prove the surviving favourite is unique by attempting a duplicate INSERT and asserting a thrown exception with SQLState 23505 — no hardcoded constraint name is used.
    - No test queries the V9 temp tables team_merge_map/player_merge_map.
    - `mvn -q verify -Pintegration -Dit.test=PostgresMigrationIT` exits 0 (all 02-01 + these 7 tests green); `mvn -q test` still 120 tests green and Docker-free.
    - Every added @Test carries GIVEN/WHEN/THEN junior-developer comments (CLAUDE.md hard rule).
  </acceptance_criteria>
  <done>The seven exhaustive V9 merge assertions pass under `mvn verify -Pintegration` and the default `mvn test` remains green and Docker-free.</done>
</task>

<task type="auto">
  <name>Task 3: Document the opt-in `mvn verify -Pintegration` command + Docker prerequisite (D-02)</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - CLAUDE.md line 200 ("**Tests:** `mvn test` (H2 in-memory; Redis disabled via `application-test.yml`).") and the "## Testing — 120 tests" section around line 206 — add the integration-test note adjacent to the existing Tests line.
    - README.md (2-line file) — add a brief testing pointer.
    - .planning/phases/02-postgres-migration-integration-tests/02-RESEARCH.md lines 371-373 (Pitfall 7: first run needs a running Docker daemon AND network to pull postgres:16-alpine; later runs use the local image cache).
    - .planning/phases/02-postgres-migration-integration-tests/02-CONTEXT.md line 27 (D-02: the profile/command MUST be clearly documented so it can be run deliberately and wired into CI).
  </read_first>
  <action>
    Update CLAUDE.md's Testing area (near line 200): after the existing `mvn test` line, add a sentence documenting that the Postgres migration integration test (PostgresMigrationIT, verifying the V8 + V9 migrations against real Postgres) is opt-in and runs via `mvn verify -Pintegration`, that it requires a running Docker daemon (Testcontainers starts an ephemeral postgres:16-alpine; first run also pulls the image), and that the default `mvn test` stays H2-only and Docker-free. Keep it consistent with the file's existing voice; note it does NOT run in the default suite. If the "## Testing — 120 tests" table's surrounding prose references the migration coverage gap (the HARD-02 caveat that V8/V9 are "unit-tested only by compile/entity-mapping"), update that note to reflect that a real-Postgres IT now covers them via `mvn verify -Pintegration`.
    Update README.md: add one short line under the description pointing at the two test commands — `mvn test` (fast H2 suite) and `mvn verify -Pintegration` (Docker-required Postgres migration IT).
    Do not touch application-test.yml, .planning/config.json, or any migration SQL.
  </action>
  <verify>
    <automated>grep -q "mvn verify -Pintegration" CLAUDE.md && grep -q "mvn verify -Pintegration" README.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md documents `mvn verify -Pintegration`, names PostgresMigrationIT, states the Docker-daemon prerequisite, and states that the default `mvn test` remains H2-only and Docker-free.
    - If present, the CLAUDE.md HARD-02 "migration verified only by compile/entity-mapping" caveat is updated to note the new real-Postgres IT covers V8/V9.
    - README.md points at both `mvn test` and `mvn verify -Pintegration`.
    - The grep verify prints DOCS_OK; application-test.yml, .planning/config.json, and migration SQL are untouched.
  </acceptance_criteria>
  <done>Both files document the opt-in integration command + Docker prerequisite and the verify grep prints DOCS_OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → ephemeral Testcontainers Postgres | Same throwaway postgres:16-alpine container as plan 02-01; this plan only adds more fixture rows and read-only assertions (plus two deliberate duplicate INSERTs that are expected to fail). No production runtime surface, no new endpoint, no real secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | Duplicate-club fixture + assertion SQL | low | mitigate | All fixture INSERTs and assertion queries use parameterized JdbcTemplate `?` placeholders (never String.format/concatenation), including the intentional collision-INSERTs that assert a 23505 unique violation. |
| T-02-05 | Information Disclosure | Documentation edits (CLAUDE.md / README.md) | low | accept | Docs describe only the public `mvn verify -Pintegration` command and Docker prerequisite; no credentials, connection strings, or secrets are written into the docs. |
| T-02-06 | Tampering | Default `mvn test` build integrity (unchanged) | low | accept | This plan adds no new dependency and does not touch the profile/Failsafe wiring from 02-01; the *IT naming keeps it out of Surefire. Re-verified by `mvn test` staying green and Docker-free at wave gate. |

No package installs occur in this plan (all deps landed in 02-01) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `mvn -q verify -Pintegration -Dit.test=PostgresMigrationIT` (Docker running): all 02-01 tests plus the seven new V9 merge tests pass — proves the remaining half of roadmap Success Criterion 3 (data merge, incl. favourites collision-skip).
- `grep -q "mvn verify -Pintegration"` in CLAUDE.md and README.md: the opt-in command + Docker prerequisite are documented — completes roadmap Success Criterion 4.
- `mvn -q test` (no profile, Docker stopped): 120 H2 tests remain green and Docker-free (no regression).
</verification>

<success_criteria>
- seedFixtures builds the full duplicate-club scenario exercising every V9 merge branch (club merge, player de-dupe, favourite re-point + collision-skip for teams and players).
- Seven exhaustive V9 merge @Test methods pass under `mvn verify -Pintegration`, with collision-skip proven behaviorally via a 23505 unique violation (no hardcoded constraint name).
- CLAUDE.md + README.md document `mvn verify -Pintegration` and its Docker prerequisite; the default `mvn test` stays green and Docker-free.
</success_criteria>

## Artifacts this plan produces

Extended test symbol (`com.onestopsports.migration.PostgresMigrationIT`):
- `seedFixtures(JdbcTemplate)` extended with the duplicate-club scenario (1 sport, 2 same-sport leagues, 2 teams sharing external_id, duplicate players, favourites covering re-point + collision branches for team and player)
- Seven new `@Test` methods: `v9_duplicateClubs_mergedIntoCanonicalRow`, `v9_teamLeagueJoinTable_populatedForCanonical`, `v9_duplicatePlayers_repointedAndDeduplicated`, `v9_favoriteTeam_repointedToCanonical_whenNoCollision`, `v9_favoriteTeam_dupDeleted_whenCanonicalAlreadyFavorited`, `v9_favoritePlayer_repointedToCanonical_whenNoCollision`, `v9_favoritePlayer_dupDeleted_whenCanonicalAlreadyFavorited`

Documentation:
- CLAUDE.md — Testing section note for `mvn verify -Pintegration` + Docker prerequisite
- README.md — testing-command pointer

No `src/main` changes. No `pom.xml` / `application-test.yml` / `.planning/config.json` changes.

<output>
Create `.planning/phases/02-postgres-migration-integration-tests/02-02-SUMMARY.md` when done.
</output>
