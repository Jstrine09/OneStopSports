---
phase: 01-backend-service-test-coverage
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/test/java/com/onestopsports/service/ApiFootballServiceTest.java
  - src/test/java/com/onestopsports/service/BallDontLieServiceTest.java
autonomous: true
requirements: [HARD-01]

must_haves:
  truths:
    - "ApiFootballService has a unit test proving happy-path mapping (searchPlayerId name-match → Optional player id; fetchPlayerStats → PlayerCareerStatsDto with the curated FOOTBALL_LABELS) AND soft-fail tests proving RestClientException → Optional.empty / null."
    - "BallDontLieService has a unit test proving happy-path mapping (first-name search + last-name match → PlayerBioDto with parsed weight/college/draft) AND a soft-fail test proving a swallowed exception → Optional.empty."
    - "Neither test makes a live network call — the RestClient chain is fully mocked with RETURNS_DEEP_STUBS."
    - "The full suite (baseline 66 + these new tests) is green via mvn test."
  artifacts:
    - "src/test/java/com/onestopsports/service/ApiFootballServiceTest.java"
    - "src/test/java/com/onestopsports/service/BallDontLieServiceTest.java"
  key_links:
    - "The 01-01 test constructors (ApiFootballService 1-arg, BallDontLieService 1-arg) and the widened package-private BdlPlayersResponse/BdlPlayer records are what let these tests compile and build fixtures — this plan depends on 01-01."
---

<objective>
Unit-test ApiFootballService and BallDontLieService following the NbaApiServiceTest deep-stub convention. BallDontLieService's happy path specifically exercises the records widened to package-private in 01-01. Assert both happy-path mapping and the swallow-exception soft-fail contract.

Purpose: Completes HARD-01 Criterion 1 for the remaining two external-API adapters, locking in the two-step football search→fetch mapping and the balldontlie first-name-search / last-name-match bio mapping, plus their soft-fail returns.
Output: Two new test classes; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-service-test-coverage/01-RESEARCH.md
@.planning/phases/01-backend-service-test-coverage/01-PATTERNS.md
@src/test/java/com/onestopsports/service/NbaApiServiceTest.java
@src/main/java/com/onestopsports/service/ApiFootballService.java
@src/main/java/com/onestopsports/service/BallDontLieService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ApiFootballServiceTest</name>
  <files>src/test/java/com/onestopsports/service/ApiFootballServiceTest.java</files>
  <read_first>
    - src/test/java/com/onestopsports/service/NbaApiServiceTest.java (deep-stub + fixture-builder convention).
    - src/main/java/com/onestopsports/service/ApiFootballService.java — method contracts: searchPlayerId(String fullName, int apiSportsLeagueId, int season) returns Optional<Integer> — Optional.empty on blank/too-short name (no HTTP), on empty response, or on RestClientException; a match is found by accent-stripped exact "firstname lastname" then loose lastname fallback (lines 123-190). fetchPlayerStats(int apiSportsPlayerId, int season) returns PlayerCareerStatsDto or null — null on empty response or RestClientException; maps to a single "season" StatCategory using FOOTBALL_LABELS (lines 202-262). IMPORTANT: both call .uri via a Function lambda (uri -> uri.path("/players").queryParam(...)...), NOT a string template — stub with .uri(any()) or .uri(any(java.util.function.Function.class)), NOT anyString(). Public records to build fixtures from: ApiResponse, ApiPlayerEntry, ApiPlayer (id, firstname, lastname), ApiStatBlock, ApiTeam, ApiLeague, ApiGames, ApiShots, ApiGoals, ApiPasses, ApiCards.
    - src/test/java/com/onestopsports/service/PlayerServiceCareerStatsTest.java (the anyInt() primitive-matcher idiom for int params; not directly reused but confirms the convention).
  </read_first>
  <action>
    Create ApiFootballServiceTest in package com.onestopsports.service with a single @Mock(answer=Answers.RETURNS_DEEP_STUBS) RestClient, constructing apiFootballService in @BeforeEach via the 01-01 test constructor ApiFootballService(restClient). Build fixtures from ApiFootballService's public records. Cover at minimum: (a) SEARCH HAPPY PATH — searchPlayerId with an ApiResponse containing an ApiPlayerEntry whose ApiPlayer firstname+lastname exactly matches the query returns Optional.of that player's id; (b) SEARCH ACCENT MATCH — a query with an accented name (e.g. a name containing é) matches an entry whose stored name is the plain-ASCII equivalent, proving the accent-strip comparison; (c) SEARCH NO-HTTP GUARD — a too-short/blank name returns Optional.empty and the RestClient body(...) stub is never triggered; (d) SEARCH SOFT-FAIL — the RestClient chain stubbed to throw a RestClientException makes searchPlayerId return Optional.empty; (e) STATS HAPPY PATH — fetchPlayerStats with an ApiResponse containing one ApiStatBlock returns a non-null PlayerCareerStatsDto whose sport is "football", whose single category uses the FOOTBALL_LABELS column set, and whose season row reflects the block's team/league; (f) STATS SOFT-FAIL — fetchPlayerStats returns null when the response is empty AND when the RestClient throws a RestClientException. Stub the fluent chain's .uri(...) with an any()-style matcher for the Function argument (do not use anyString()). AssertJ only; each test method carries a plain-English inline comment (CLAUDE.md rule).
  </action>
  <verify>
    <automated>mvn -q test -Dtest=ApiFootballServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - ApiFootballServiceTest.java exists and is annotated @ExtendWith(MockitoExtension.class), constructing the service via the 01-01 single-RestClient test constructor.
    - It contains a happy-path test asserting searchPlayerId returns Optional.of(expectedId) on a name match (including one accent-insensitive case).
    - It contains a soft-fail test asserting searchPlayerId returns Optional.empty when the RestClient throws a RestClientException.
    - It contains a happy-path test asserting fetchPlayerStats maps a stat block into a PlayerCareerStatsDto with sport "football" and the FOOTBALL_LABELS columns.
    - It contains a soft-fail test asserting fetchPlayerStats returns null on RestClientException.
    - The RestClient .uri(...) stub uses an any()-form matcher for the Function argument (not anyString()); no @SpringBootTest; no live HTTP.
    - `mvn -q test -Dtest=ApiFootballServiceTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

<task type="auto">
  <name>Task 2: Write BallDontLieServiceTest</name>
  <files>src/test/java/com/onestopsports/service/BallDontLieServiceTest.java</files>
  <read_first>
    - src/test/java/com/onestopsports/service/NbaApiServiceTest.java (deep-stub + varargs .uri matcher form any(String.class), any(Object[].class)).
    - src/main/java/com/onestopsports/service/BallDontLieService.java — searchPlayerByName(String) contract (lines 52-113): blank/null name → Optional.empty; searches by first name, picks the result whose lastName equals ours (case-insensitive), else falls back to the first result only when the last name is blank, else Optional.empty; parses the String weight to Integer; catches broad Exception → Optional.empty. The call uses .uri("/players?search={first}&per_page=10", firstName) — a varargs template. Depends on the package-private BdlPlayersResponse(List<BdlPlayer> data) and BdlPlayer canonical constructor (component order: id, firstName, lastName, position, height, weight, jerseyNumber, college, country, draftYear, draftRound, draftNumber) — both widened to package-private in 01-01.
  </read_first>
  <action>
    Create BallDontLieServiceTest in package com.onestopsports.service with a single @Mock(answer=Answers.RETURNS_DEEP_STUBS) RestClient, constructing ballDontLieService in @BeforeEach via the 01-01 test constructor BallDontLieService(restClient). Build fixtures by constructing BallDontLieService.BdlPlayersResponse and BallDontLieService.BdlPlayer directly (now possible because 01-01 widened them to package-private) — pass all canonical components in declared order, using the string weight form (e.g. a numeric string) so the parse path is exercised. Cover at minimum: (a) HAPPY PATH — searchPlayerByName("Jaylen Brown") stubs the RestClient to return a response containing a BdlPlayer with firstName "Jaylen" and lastName "Brown" and returns Optional.of a PlayerBioDto with the height, parsed integer weight, college, and draft fields mapped through; (b) LAST-NAME MISMATCH — a first-name hit whose lastName does not match returns Optional.empty; (c) BLANK-NAME GUARD — a null or blank name returns Optional.empty without stubbing/triggering the RestClient body call; (d) SOFT-FAIL — the RestClient chain stubbed to throw an exception makes searchPlayerByName return Optional.empty (no propagation). Use the varargs matcher any(String.class), any(Object[].class) for the .uri(template, firstName) call. AssertJ only; each test method carries a plain-English inline comment (CLAUDE.md rule).
  </action>
  <verify>
    <automated>mvn -q test -Dtest=BallDontLieServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - BallDontLieServiceTest.java exists and is annotated @ExtendWith(MockitoExtension.class), constructing the service via the 01-01 single-RestClient test constructor, and it directly constructs BdlPlayersResponse/BdlPlayer fixtures (proving the 01-01 record widening).
    - It contains a happy-path test asserting searchPlayerByName maps a first-name-search + last-name-match result into a PlayerBioDto with parsed integer weight and the college/draft fields populated.
    - It contains a soft-fail test asserting searchPlayerByName returns Optional.empty when the RestClient throws.
    - It contains a guard test asserting a blank/null name returns Optional.empty without a network call.
    - No @SpringBootTest; no live HTTP.
    - `mvn -q test -Dtest=BallDontLieServiceTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No runtime code, no endpoint, no new external call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Denial of Service (regression guard) | ApiFootballService / BallDontLieService soft-fail contract | low | mitigate | Tests pin the existing control that a failed or rate-limited upstream call (429/500/network) is swallowed to Optional.empty / null and never surfaces as a client 500. This guards the free-tier failure path against future regressions. |

No new attack surface, no dependency installs. Tests make zero network calls (RestClient fully mocked).
</threat_model>

<verification>
- `mvn -q test -Dtest=ApiFootballServiceTest` and `mvn -q test -Dtest=BallDontLieServiceTest` each exit 0.
- At wave merge: `mvn test` — baseline 66 + the new tests all green.
</verification>

<success_criteria>
- ApiFootballServiceTest and BallDontLieServiceTest each cover happy-path mapping AND swallow-exception soft-fail with no live network calls.
- Full suite green.
</success_criteria>

## Artifacts this plan produces

New test files:
- `src/test/java/com/onestopsports/service/ApiFootballServiceTest.java` — tests for searchPlayerId (exact + accent match, too-short guard, RestClientException → Optional.empty) and fetchPlayerStats (FOOTBALL_LABELS mapping, empty/RestClientException → null)
- `src/test/java/com/onestopsports/service/BallDontLieServiceTest.java` — tests for searchPlayerByName (first-name-search/last-name-match → PlayerBioDto, mismatch → empty, blank guard, exception → empty)

Plus per-class private fixture-builder helpers. No new production symbols.

<output>
Create `.planning/phases/01-backend-service-test-coverage/01-03-SUMMARY.md` when done.
</output>
