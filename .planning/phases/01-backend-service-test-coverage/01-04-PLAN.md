---
phase: 01-backend-service-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/onestopsports/service/UserServiceTest.java
  - src/test/java/com/onestopsports/service/SportServiceTest.java
autonomous: true
requirements: [HARD-01]

must_haves:
  truths:
    - "UserService favourite CRUD is unit-tested: 404 (ResponseStatusException) on missing user/team/player, the skip-if-already-favourited guard (no save), and the happy-path save/list/remove delegations."
    - "SportService is unit-tested: getAllSports maps repository rows to SportDto, and getSportBySlug returns a SportDto on hit and throws ResponseStatusException(404) on miss."
    - "Both tests use plain @InjectMocks / @Mock (no test constructor, no Spring context, no HTTP)."
    - "The full suite (baseline 66 + these new tests) is green via mvn test."
  artifacts:
    - "src/test/java/com/onestopsports/service/UserServiceTest.java"
    - "src/test/java/com/onestopsports/service/SportServiceTest.java"
  key_links:
    - "UserService reuses TeamService.toDto and PlayerService.toDto for favourite listing — these collaborators are mocked, so the test asserts delegation without touching the real mappers."
---

<objective>
Unit-test UserService (favourite teams/players CRUD, profile lookup) and SportService (sport listing + slug lookup) using the plain @InjectMocks/@Mock convention from MatchServiceTest — no test constructor needed because both constructors already accept pure Spring-bean types.

Purpose: HARD-01 Criterion 2 (partial) — covers the two previously-untested plain services, pinning their 404 guards, the skip-if-already-favourited behavior, and DTO mapping.
Output: Two new test classes; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-service-test-coverage/01-RESEARCH.md
@.planning/phases/01-backend-service-test-coverage/01-PATTERNS.md
@src/test/java/com/onestopsports/service/MatchServiceTest.java
@src/test/java/com/onestopsports/service/PlayerServiceCareerStatsTest.java
@src/main/java/com/onestopsports/service/UserService.java
@src/main/java/com/onestopsports/service/SportService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write UserServiceTest</name>
  <files>src/test/java/com/onestopsports/service/UserServiceTest.java</files>
  <read_first>
    - src/test/java/com/onestopsports/service/MatchServiceTest.java (the @InjectMocks/@Mock convention, the never() guard idiom, AssertJ assertThatThrownBy usage).
    - src/main/java/com/onestopsports/service/UserService.java (full file — 7 constructor deps: UserRepository, TeamRepository, PlayerRepository, FavoriteTeamRepository, FavoritePlayerRepository, TeamService, PlayerService; findUser throws ResponseStatusException(NOT_FOUND) on unknown username; addFavoriteTeam returns early when favoriteTeamRepository.existsByUserIdAndTeamId is true, else throws 404 if teamRepository.findById is empty, else saves a FavoriteTeam; addFavoritePlayer is the symmetric shape; getFavoriteTeams/getFavoritePlayers map repo rows through teamService.toDto / playerService.toDto; remove* delegate to deleteByUserIdAndTeamId / deleteByUserIdAndPlayerId).
    - src/main/java/com/onestopsports/model/UserAccount.java, FavoriteTeam.java, FavoriteTeam and Team/Player builders (to build fixture entities with the @Builder quartet).
  </read_first>
  <action>
    Create UserServiceTest in package com.onestopsports.service, @ExtendWith(MockitoExtension.class), with @Mock fields for all seven dependencies and @InjectMocks UserService. Build fixture UserAccount/Team/Player entities via their Lombok builders. Cover at minimum: (a) getCurrentUser happy path returns a UserDto with the user's id/username/email; (b) getCurrentUser with an unknown username asserts assertThatThrownBy(...).isInstanceOf(ResponseStatusException.class) and that its message contains "User not found"; (c) addFavoriteTeam when existsByUserIdAndTeamId is true returns without calling teamRepository.findById and without favoriteTeamRepository.save (verify never() on both); (d) addFavoriteTeam when the team is missing (teamRepository.findById empty) throws ResponseStatusException 404 and does not save; (e) addFavoriteTeam happy path saves exactly one FavoriteTeam (verify save called once); (f) at least one symmetric addFavoritePlayer test (skip-if-exists OR 404-on-missing) to prove the player path; (g) getFavoriteTeams maps favourite rows through teamService.toDto (stub the mock to return a known TeamDto and assert it appears in the result); (h) removeFavoriteTeam delegates to favoriteTeamRepository.deleteByUserIdAndTeamId with the resolved user id and team id. Stub userRepository.findByUsername to return the fixture user for the happy paths. AssertJ only; every test method carries a plain-English inline comment (CLAUDE.md rule).
  </action>
  <verify>
    <automated>mvn -q test -Dtest=UserServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - UserServiceTest.java exists, is @ExtendWith(MockitoExtension.class), declares @Mock for all 7 UserService dependencies and @InjectMocks UserService.
    - It contains a test asserting getCurrentUser throws ResponseStatusException (message contains "User not found") for an unknown username.
    - It contains a test asserting addFavoriteTeam does NOT save (verify(favoriteTeamRepository, never()).save(...)) when the team is already favourited.
    - It contains a test asserting addFavoriteTeam throws ResponseStatusException 404 when the team does not exist, and a happy-path test asserting exactly one FavoriteTeam is saved.
    - It contains at least one addFavoritePlayer test and one getFavoriteTeams delegation test (result includes the TeamDto returned by the mocked teamService.toDto).
    - No @SpringBootTest; no HTTP; no real DB.
    - `mvn -q test -Dtest=UserServiceTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

<task type="auto">
  <name>Task 2: Write SportServiceTest</name>
  <files>src/test/java/com/onestopsports/service/SportServiceTest.java</files>
  <read_first>
    - src/test/java/com/onestopsports/service/MatchServiceTest.java (scaled-down @InjectMocks/@Mock convention).
    - src/main/java/com/onestopsports/service/SportService.java (full file — one dependency SportRepository; getAllSports maps findAll → SportDto via private toDto; getSportBySlug uses findBySlug(...).map(toDto).orElseThrow(ResponseStatusException NOT_FOUND "Sport not found: " + slug)).
    - src/main/java/com/onestopsports/model/Sport.java (builder fields id/name/slug/iconUrl for fixtures).
  </read_first>
  <action>
    Create SportServiceTest in package com.onestopsports.service, @ExtendWith(MockitoExtension.class), with @Mock SportRepository and @InjectMocks SportService. Cover: (a) getAllSports maps a repository list of two Sport entities to two SportDto values preserving id/name/slug/iconUrl (stub sportRepository.findAll); (b) getAllSports returns an empty list when the repository returns none; (c) getSportBySlug happy path returns the mapped SportDto when findBySlug returns a Sport; (d) getSportBySlug throws ResponseStatusException (message contains "Sport not found") when findBySlug returns Optional.empty — assert via assertThatThrownBy. Build Sport fixtures with the Lombok builder. AssertJ only; each test method carries a plain-English inline comment (CLAUDE.md rule).
  </action>
  <verify>
    <automated>mvn -q test -Dtest=SportServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - SportServiceTest.java exists, is @ExtendWith(MockitoExtension.class), with @Mock SportRepository and @InjectMocks SportService.
    - It contains a getAllSports mapping test asserting the SportDto fields match the source Sport entities, and an empty-list test.
    - It contains a getSportBySlug happy-path test and a test asserting ResponseStatusException (message contains "Sport not found") on a missing slug.
    - No @SpringBootTest; no HTTP; no real DB.
    - `mvn -q test -Dtest=SportServiceTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No runtime code, no endpoint, no new external call. UserService's favourites are JWT-guarded at the controller/SecurityConfig layer, which is out of this phase's scope and already covered by the baseline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering (regression guard) | UserService favourite-CRUD guard clauses | low | mitigate | Tests pin the existing behavior that missing user/team/player yields a controlled 404 (ResponseStatusException) and that duplicate favourites are skipped rather than causing a DB unique-constraint error — behavior, not access control. Access control (JWT on /api/users/me/**) is unchanged and out of scope. |

No new attack surface, no dependency installs, no network calls.
</threat_model>

<verification>
- `mvn -q test -Dtest=UserServiceTest` and `mvn -q test -Dtest=SportServiceTest` each exit 0.
- At wave merge: `mvn test` — baseline 66 + the new tests all green.
</verification>

<success_criteria>
- UserServiceTest covers 404 guards, skip-if-already-favourited, and happy-path save/list/remove.
- SportServiceTest covers listing, mapping, and slug 404.
- Full suite green.
</success_criteria>

## Artifacts this plan produces

New test files:
- `src/test/java/com/onestopsports/service/UserServiceTest.java` — getCurrentUser (happy + 404), addFavoriteTeam (skip-if-exists, 404, happy save), addFavoritePlayer (symmetric), getFavoriteTeams (delegation), removeFavoriteTeam (delegation)
- `src/test/java/com/onestopsports/service/SportServiceTest.java` — getAllSports (mapping + empty), getSportBySlug (happy + 404)

No new production symbols.

<output>
Create `.planning/phases/01-backend-service-test-coverage/01-04-SUMMARY.md` when done.
</output>
