---
phase: 01-backend-service-test-coverage
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/test/java/com/onestopsports/service/NflApiServiceTest.java
  - src/test/java/com/onestopsports/service/ExternalApiServiceTest.java
autonomous: true
requirements: [HARD-01]

must_haves:
  truths:
    - "NflApiService has a unit test proving happy-path mapping (ESPN scoreboard/standings JSON records → MatchDto/StandingsEntryDto) AND a soft-fail test proving a swallowed RestClientException returns an empty list / null rather than propagating."
    - "ExternalApiService has a unit test proving happy-path standings mapping (TOTAL group → StandingsEntryDto) AND a soft-fail test proving a swallowed exception returns an empty list / null."
    - "Neither test makes a live network call — the RestClient chain is fully mocked with RETURNS_DEEP_STUBS."
    - "The full suite (baseline 66 + these new tests) is green via mvn test."
  artifacts:
    - "src/test/java/com/onestopsports/service/NflApiServiceTest.java"
    - "src/test/java/com/onestopsports/service/ExternalApiServiceTest.java"
  key_links:
    - "The package-private test constructors added in 01-01 (NflApiService 3-arg, ExternalApiService 2-arg) are what let these tests inject mock RestClients — this plan cannot compile without 01-01."
---

<objective>
Unit-test NflApiService and ExternalApiService following the canonical NbaApiServiceTest deep-stub convention: construct the real service with mock RestClient(s) via the 01-01 test constructors, build fixture responses from the services' own public response records, and assert both happy-path DTO mapping and the swallow-RestClientException soft-fail contract.

Purpose: Locks in the soft-fail contract (RestClientException → empty list / null, never a propagated 500) and the mapping behavior for two of the four previously-untested external-API adapters, per HARD-01 Criterion 1.
Output: Two new test classes; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-service-test-coverage/01-RESEARCH.md
@.planning/phases/01-backend-service-test-coverage/01-PATTERNS.md
@src/test/java/com/onestopsports/service/NbaApiServiceTest.java
@src/main/java/com/onestopsports/service/NflApiService.java
@src/main/java/com/onestopsports/service/ExternalApiService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write NflApiServiceTest</name>
  <files>src/test/java/com/onestopsports/service/NflApiServiceTest.java</files>
  <read_first>
    - src/test/java/com/onestopsports/service/NbaApiServiceTest.java (full file — the exact analog: @SuppressWarnings unchecked/rawtypes + @ExtendWith(MockitoExtension.class); three @Mock(answer=Answers.RETURNS_DEEP_STUBS) RestClient fields; @BeforeEach constructing via the test constructor; private fixture-builder helpers that build response records directly; assertThat + when(...).thenReturn/thenThrow; the varargs-uri matcher pattern any(String.class), any(Object[].class)).
    - src/main/java/com/onestopsports/service/NflApiService.java — method contracts to cover: fetchGameDtosByDate(LocalDate, Long) maps EspnScoreboardResponse → List<MatchDto> and returns empty on null body (this method has NO try/catch, so do NOT assert exception-swallow on it); fetchStandings(Long) wraps RestClientException → empty list (lines 436-500); fetchCareerStats(String) wraps RestClientException → null (lines 514-533). Public response records to build fixtures from: EspnScoreboardResponse, EspnEvent, EspnEventStatus, EspnStatusType, EspnCompetition, EspnCompetitor, EspnCompTeam, EspnStandingsResponse, EspnConference, EspnConferenceStandings, EspnStandingsEntry, EspnStandingsTeam, EspnStat, EspnLogo.
  </read_first>
  <action>
    Create NflApiServiceTest in package com.onestopsports.service. Mirror NbaApiServiceTest's class-level annotations and the three RETURNS_DEEP_STUBS RestClient mocks (restClient, standingsClient, statsClient); construct nflApiService in @BeforeEach via the package-private test constructor added in 01-01. Write private fixture-builder helpers that construct NflApiService's own public Espn* records directly (no JSON strings), mirroring NbaApiServiceTest.scoreboardWith and standingsWithTwo. Cover at minimum: (a) HAPPY PATH — fetchGameDtosByDate returns a mapped MatchDto for a STATUS_FINAL event, asserting mapped status FINISHED, parsed integer home/away scores, timezone "ET", and the passed-in dbLeagueId on the MatchDto; (b) NULL-BODY GUARD — fetchGameDtosByDate with a null body stub returns an empty list; (c) STANDINGS HAPPY PATH — fetchStandings maps a two-conference fixture into ranked StandingsEntryDto rows with conference and division populated and the dbLeagueId set on each team; (d) SOFT-FAIL — fetchStandings with the standingsClient stubbed to throw a RestClientException returns an empty list WITHOUT throwing; (e) SOFT-FAIL — fetchCareerStats with statsClient stubbed to throw a RestClientException returns null. Use the varargs matcher form any(String.class), any(Object[].class) for any .uri(template, args) call and anyString() for single-string .uri(...) calls, exactly as the analog distinguishes them. Use AssertJ assertions only. Every new test method needs a plain-English inline comment explaining what behavior it pins (CLAUDE.md rule for new files).
  </action>
  <verify>
    <automated>mvn -q test -Dtest=NflApiServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - NflApiServiceTest.java exists in src/test/java/com/onestopsports/service/ and is annotated @ExtendWith(MockitoExtension.class).
    - It contains a happy-path test asserting fetchGameDtosByDate maps a STATUS_FINAL event to a MatchDto with status "FINISHED" and integer scores.
    - It contains a soft-fail test asserting fetchStandings returns an empty list when standingsClient throws a RestClientException (no exception propagates).
    - It contains a soft-fail test asserting fetchCareerStats returns null when statsClient throws a RestClientException.
    - No @SpringBootTest / @ActiveProfiles annotation appears (pure Mockito unit test, no Spring context, no live HTTP).
    - `mvn -q test -Dtest=NflApiServiceTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

<task type="auto">
  <name>Task 2: Write ExternalApiServiceTest</name>
  <files>src/test/java/com/onestopsports/service/ExternalApiServiceTest.java</files>
  <read_first>
    - src/test/java/com/onestopsports/service/NbaApiServiceTest.java (deep-stub convention, fixture-builder style).
    - src/main/java/com/onestopsports/service/ExternalApiService.java — method contracts: fetchStandings(Integer) filters the "TOTAL" standings group and maps ApiStandingEntry → StandingsEntryDto, catching broad Exception → empty list (lines 231-254); fetchFootballBoxScore(Long) catches Exception → null and returns null on null detail (lines 476-573). Public records to build fixtures from: ApiStandingsResponse, ApiStandingGroup, ApiStandingEntry, ApiMatchTeam, ApiMatchDetail. Note the football→drawn field naming: ApiStandingEntry.draw() maps to StandingsEntryDto.drawn().
    - src/test/java/com/onestopsports/service/MatchServiceTest.java line ~110 (the anyInt() primitive-matcher note — fetchMatchDtosByCompetition takes a primitive int).
  </read_first>
  <action>
    Create ExternalApiServiceTest in package com.onestopsports.service. Use ONE @Mock(answer=Answers.RETURNS_DEEP_STUBS) RestClient plus a plain @Mock LeagueRepository; construct externalApiService in @BeforeEach via the 01-01 test constructor ExternalApiService(restClient, leagueRepository). Build fixtures from ExternalApiService's public Api* records. Cover at minimum: (a) HAPPY PATH — fetchStandings builds an ApiStandingsResponse containing a "TOTAL" ApiStandingGroup with one ApiStandingEntry, stubs the RestClient chain to return it, and asserts the resulting StandingsEntryDto carries the entry's position, won, drawn (from draw), lost, goalsFor/goalsAgainst, and points, with conference/division/pct/gamesBehind all null (football has no such concepts); (b) FILTER — a response whose only group has type other than "TOTAL" yields an empty list; (c) SOFT-FAIL — the RestClient chain stubbed to throw a RestClientException makes fetchStandings return an empty list without throwing; (d) BOX-SCORE SOFT-FAIL — fetchFootballBoxScore returns null when the RestClient chain throws, and returns null when the stubbed detail body is null. The standings endpoint is called via fetchStandingsByCompetition which uses .uri("/competitions/{id}/standings", competitionId) — a varargs template — so use the any(String.class), any(Object[].class) matcher form. AssertJ only. Each test method carries a plain-English inline comment (CLAUDE.md rule).
  </action>
  <verify>
    <automated>mvn -q test -Dtest=ExternalApiServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - ExternalApiServiceTest.java exists and is annotated @ExtendWith(MockitoExtension.class), with a mocked RestClient and a mocked LeagueRepository injected via the 01-01 test constructor.
    - It contains a happy-path test asserting fetchStandings maps a TOTAL-group ApiStandingEntry to a StandingsEntryDto with the correct position/won/drawn/lost and null conference/division/pct/gamesBehind.
    - It contains a soft-fail test asserting fetchStandings returns an empty list when the RestClient throws a RestClientException.
    - It contains a soft-fail test asserting fetchFootballBoxScore returns null when the RestClient throws (and on null detail body).
    - No @SpringBootTest / @ActiveProfiles; no live HTTP.
    - `mvn -q test -Dtest=ExternalApiServiceTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. Adds no runtime code, no endpoint, no new external call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Denial of Service (regression guard) | NflApiService / ExternalApiService soft-fail contract | low | mitigate | These tests exist specifically to lock in the existing control: a failed upstream call is swallowed to an empty list / null and never propagated to a client-facing 500. Asserting the RestClientException path preserves that control against future refactors. |

No new attack surface, no dependency installs. Tests make zero network calls (RestClient fully mocked), satisfying the "no live network calls" security/functional requirement.
</threat_model>

<verification>
- `mvn -q test -Dtest=NflApiServiceTest` and `mvn -q test -Dtest=ExternalApiServiceTest` each exit 0.
- At wave merge: `mvn test` — baseline 66 + the new tests all green.
</verification>

<success_criteria>
- NflApiServiceTest and ExternalApiServiceTest each cover happy-path mapping AND swallow-RestClientException soft-fail with no live network calls.
- Full suite green.
</success_criteria>

## Artifacts this plan produces

New test files:
- `src/test/java/com/onestopsports/service/NflApiServiceTest.java` — tests for fetchGameDtosByDate (mapping + null-body), fetchStandings (mapping + RestClientException soft-fail), fetchCareerStats (RestClientException → null)
- `src/test/java/com/onestopsports/service/ExternalApiServiceTest.java` — tests for fetchStandings (TOTAL-group mapping + filter + RestClientException soft-fail), fetchFootballBoxScore (null soft-fail)

Plus per-class private fixture-builder helper methods (test-local, mirroring NbaApiServiceTest's scoreboardWith/standingsWithTwo). No new production symbols.

<output>
Create `.planning/phases/01-backend-service-test-coverage/01-02-SUMMARY.md` when done.
</output>
