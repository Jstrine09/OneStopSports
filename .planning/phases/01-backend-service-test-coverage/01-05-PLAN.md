---
phase: 01-backend-service-test-coverage
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/onestopsports/service/PlayerServiceTest.java
  - src/test/java/com/onestopsports/controller/GlobalExceptionHandlerTest.java
autonomous: true
requirements: [HARD-01]

must_haves:
  truths:
    - "PlayerService.resolvePhotoUrl is unit-tested through its public entry points across all three branches: persisted photoUrl wins; basketball + externalId derives the ESPN NBA CDN URL; american-football + externalId derives the ESPN NFL CDN URL; no externalId (and non-NBA/NFL) yields null."
    - "PlayerService.toDto maps every PlayerDto field (id/name/position/nationality/dateOfBirth/jerseyNumber/photoUrl/teamId), and searchPlayers normalizes the query, delegates to findByNameNormalizedContaining, and caps the result at 10."
    - "GlobalExceptionHandler's documented status mappings are asserted, AND the ResponseStatusException-passthrough-precedes-Exception-catch-all requirement is proven via real Spring MVC dispatch (standaloneSetup), not by calling handler methods directly."
    - "The full suite (baseline 66 + these new tests) is green via mvn test."
  artifacts:
    - "src/test/java/com/onestopsports/service/PlayerServiceTest.java"
    - "src/test/java/com/onestopsports/controller/GlobalExceptionHandlerTest.java"
  key_links:
    - "The dispatch-order guarantee is a Spring @ExceptionHandler resolution fact, not source-file order — it is only provable by exercising MockMvcBuilders.standaloneSetup(...).setControllerAdvice(new GlobalExceptionHandler())."
---

<objective>
Cover the three remaining untested PlayerService code paths (resolvePhotoUrl, toDto, searchPlayers) in a NEW PlayerServiceTest (leaving the existing PlayerServiceCareerStatsTest untouched), and unit-test GlobalExceptionHandler — with the ResponseStatusException-before-catch-all requirement proven via real MVC dispatch rather than direct method calls.

Purpose: Completes HARD-01 Criteria 2 (GlobalExceptionHandler incl. passthrough ordering) and 3 (PlayerService photo/DTO/search).
Output: Two new test classes; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-service-test-coverage/01-RESEARCH.md
@.planning/phases/01-backend-service-test-coverage/01-PATTERNS.md
@src/test/java/com/onestopsports/service/PlayerServiceCareerStatsTest.java
@src/test/java/com/onestopsports/controller/AuthControllerTest.java
@src/main/java/com/onestopsports/service/PlayerService.java
@src/main/java/com/onestopsports/controller/GlobalExceptionHandler.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PlayerServiceTest (resolvePhotoUrl, toDto, searchPlayers)</name>
  <files>src/test/java/com/onestopsports/service/PlayerServiceTest.java</files>
  <read_first>
    - src/test/java/com/onestopsports/service/PlayerServiceCareerStatsTest.java (the sibling test for the same class — reuse its @Mock set + @InjectMocks shape and duplicate a minimal private playerInSport-style fixture helper that builds a Player → Team → Sport chain; do NOT edit this existing file).
    - src/main/java/com/onestopsports/service/PlayerService.java — method contracts: toDto (lines 185-195) maps Player → PlayerDto(id, name, position, nationality, dateOfBirth, jerseyNumber, resolvePhotoUrl(player), team.id()); resolvePhotoUrl (lines 214-237) is three layers: (1) non-blank player.photoUrl wins; (2) with a non-blank externalId, basketball → "https://a.espncdn.com/i/headshots/nba/players/full/{externalId}.png", american-football → ".../nfl/...", default → null; also null when externalId blank or team/sport null; searchPlayers (lines 176-182) normalizes via TextNormalizer.normalize(query), calls playerRepository.findByNameNormalizedContaining(normalized), .limit(10), maps toDto. getPlayerById (lines 45-49) is the public entry point returning toDto or 404.
    - src/main/java/com/onestopsports/util/TextNormalizer.java (to know what normalize(query) produces so the findByNameNormalizedContaining stub arg is correct — or stub with anyString()).
    - src/main/java/com/onestopsports/model/Player.java, Team.java, Sport.java (builders for fixtures; Team.addLeague / Team.sport as used by the existing career-stats helper).
  </read_first>
  <action>
    Create PlayerServiceTest in package com.onestopsports.service, @ExtendWith(MockitoExtension.class), with @Mock PlayerRepository, BallDontLieService, NbaApiService, NflApiService, ApiFootballService and @InjectMocks PlayerService. Add a private helper that builds a Player with a Team that has a Sport (slug), an externalId, and optionally a persisted photoUrl (mirror PlayerServiceCareerStatsTest.playerInSport but add photoUrl control). Exercise resolvePhotoUrl and toDto THROUGH the public getPlayerById entry point (stub playerRepository.findById to return the fixture, then assert on the returned PlayerDto). Cover at minimum: (a) PERSISTED WINS — a player with a non-blank photoUrl returns that exact URL regardless of sport/externalId; (b) NBA DERIVE — a basketball player with externalId "1966" and no photoUrl yields photoUrl ending "/i/headshots/nba/players/full/1966.png"; (c) NFL DERIVE — an american-football player with externalId "3139477" and no photoUrl yields the nfl CDN URL; (d) NULL — a player with no photoUrl and no externalId (or a non-NBA/NFL sport such as football) yields null photoUrl; (e) TODO MAPPING — assert toDto (via getPlayerById) maps name/position/nationality/dateOfBirth/jerseyNumber and team id correctly; (f) SEARCH — stub playerRepository.findByNameNormalizedContaining to return MORE than 10 players and assert searchPlayers returns exactly 10 mapped PlayerDto values (limit cap), and that the query was normalized (assert the stubbed argument matches TextNormalizer.normalize(query) or use an argument captor / anyString()). AssertJ only; each test method carries a plain-English inline comment (CLAUDE.md rule).
  </action>
  <verify>
    <automated>mvn -q test -Dtest=PlayerServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - PlayerServiceTest.java exists as a NEW file (PlayerServiceCareerStatsTest.java is unmodified), @ExtendWith(MockitoExtension.class), @InjectMocks PlayerService with the five collaborators mocked.
    - It contains resolvePhotoUrl tests for all three branches: persisted-photoUrl-wins, basketball→NBA CDN URL, american-football→NFL CDN URL, and a null case (no externalId / unsupported sport).
    - It contains a toDto mapping test asserting the non-photo PlayerDto fields and team id.
    - It contains a searchPlayers test asserting the result is capped at 10 and the query is normalized before the repository lookup.
    - No @SpringBootTest; no HTTP; no real DB.
    - `mvn -q test -Dtest=PlayerServiceTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

<task type="auto">
  <name>Task 2: Write GlobalExceptionHandlerTest (dispatch-order + handler mappings)</name>
  <files>src/test/java/com/onestopsports/controller/GlobalExceptionHandlerTest.java</files>
  <read_first>
    - src/main/java/com/onestopsports/controller/GlobalExceptionHandler.java (full file — the 9 handlers and their exact status/body: MethodArgumentNotValidException→400, HttpMessageNotReadableException→400 "Malformed or missing request body", MethodArgumentTypeMismatchException→400, MissingServletRequestParameterException→400, HttpRequestMethodNotSupportedException→405, BadCredentialsException→401 "Invalid username or password", AccessDeniedException→403, ResponseStatusException→passthrough of its own status + reason, DataIntegrityViolationException→409, Exception→500 "An unexpected error occurred"; body shape is ErrorResponseDto with fields status/error/message/timestamp).
    - src/test/java/com/onestopsports/controller/AuthControllerTest.java (the MockMvc + jsonPath + status matcher static-import style to reuse — but do NOT copy its @WebMvcTest/@Import(SecurityConfig.class); this test uses the lighter standaloneSetup instead).
    - RESEARCH.md Pitfall 1 and Code Examples "GlobalExceptionHandler — recommended standaloneSetup dispatch test" (the exact ThrowingTestController + standaloneSetup pattern to follow).
  </read_first>
  <action>
    Create GlobalExceptionHandlerTest in package com.onestopsports.controller. Define a small test-only @RestController with two endpoints: one that throws new ResponseStatusException(HttpStatus.NOT_FOUND, "Player not found: 999") and one that throws a raw RuntimeException. Build a MockMvc via MockMvcBuilders.standaloneSetup(thatController).setControllerAdvice(new GlobalExceptionHandler()).build(). Write the DISPATCH-ORDER tests: hitting the ResponseStatusException endpoint asserts status is 404 and jsonPath $.message equals "Player not found: 999" (proving Spring routes to handleResponseStatus, i.e. the passthrough precedes the Exception catch-all); hitting the RuntimeException endpoint asserts status is 500 and jsonPath $.message equals "An unexpected error occurred" (proving the catch-all still fires for genuinely unhandled cases, and that it does NOT leak the raw exception message). ADDITIONALLY, cover the directly-constructible handlers with plain unit calls on new GlobalExceptionHandler(): assert handleBadCredentials returns 401 with message "Invalid username or password", handleAccessDenied returns 403, handleMethodNotSupported (construct HttpRequestMethodNotSupportedException with a verb) returns 405, handleMissingParam (construct MissingServletRequestParameterException) returns 400 with a message naming the parameter, handleUnreadable returns 400 "Malformed or missing request body", and handleDataIntegrity returns 409. Assert on the ResponseEntity status code and the ErrorResponseDto body fields. AssertJ (and MockMvc's andExpect for the dispatch tests); each test method carries a plain-English inline comment (CLAUDE.md rule). Note the two hardest-to-construct exceptions (MethodArgumentNotValidException, MethodArgumentTypeMismatchException) are already covered indirectly by the baseline AuthControllerTest and may be exercised through the dispatch controller or omitted from direct-call coverage — planner's discretion, but the ResponseStatusException-vs-catch-all dispatch test is mandatory.
  </action>
  <verify>
    <automated>mvn -q test -Dtest=GlobalExceptionHandlerTest</automated>
  </verify>
  <acceptance_criteria>
    - GlobalExceptionHandlerTest.java exists in src/test/java/com/onestopsports/controller/.
    - It builds MockMvc via MockMvcBuilders.standaloneSetup(...).setControllerAdvice(new GlobalExceptionHandler()) and contains a test asserting a thrown ResponseStatusException(NOT_FOUND) dispatches to the passthrough handler (status 404, message passed through) — NOT to the catch-all.
    - It contains a test asserting a thrown raw RuntimeException dispatches to the catch-all (status 500, body message "An unexpected error occurred", raw exception message not leaked).
    - It contains direct-call tests asserting the status/body of at least handleBadCredentials (401), handleAccessDenied (403), handleMethodNotSupported (405), handleMissingParam (400), handleUnreadable (400), and handleDataIntegrity (409).
    - `mvn -q test -Dtest=GlobalExceptionHandlerTest` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria above are met and the task's automated verify command exits 0 with the full suite (baseline 66 tests) still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API error surface | Untrusted requests can trigger exceptions; GlobalExceptionHandler is the control that shapes what leaves the server. This plan tests (does not change) that control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure (regression guard) | GlobalExceptionHandler catch-all | low | mitigate | The catch-all dispatch test explicitly asserts the client-facing body is the generic "An unexpected error occurred" and does NOT contain the raw exception message — pinning the existing control that internal detail is logged server-side only, never returned to the client. |

No new attack surface, no dependency installs, no network calls (standaloneSetup runs in-process with no live server).
</threat_model>

<verification>
- `mvn -q test -Dtest=PlayerServiceTest` and `mvn -q test -Dtest=GlobalExceptionHandlerTest` each exit 0.
- At wave merge: `mvn test` — baseline 66 + the new tests all green.
</verification>

<success_criteria>
- PlayerServiceTest covers all three resolvePhotoUrl branches, toDto mapping, and the searchPlayers normalize+limit(10) behavior.
- GlobalExceptionHandlerTest proves the ResponseStatusException-before-catch-all dispatch via standaloneSetup and covers the documented status mappings.
- Full suite green.
</success_criteria>

## Artifacts this plan produces

New test files:
- `src/test/java/com/onestopsports/service/PlayerServiceTest.java` — resolvePhotoUrl (persisted / NBA CDN / NFL CDN / null), toDto mapping, searchPlayers (normalize + limit 10)
- `src/test/java/com/onestopsports/controller/GlobalExceptionHandlerTest.java` — standaloneSetup dispatch tests (ResponseStatusException passthrough vs RuntimeException catch-all) + direct-call handler-mapping tests (401/403/405/400/400/409). Contains a test-only inner @RestController (ThrowingTestController) used purely as a dispatch fixture.

No new production symbols.

<output>
Create `.planning/phases/01-backend-service-test-coverage/01-05-SUMMARY.md` when done.
</output>
